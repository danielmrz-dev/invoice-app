import { InvoiceStatusColorDirective } from './invoice-status-color.directive';

describe('InvoiceStatusColorDirective', () => {
  it('should create an instance', () => {
    const directive = new InvoiceStatusColorDirective();
    expect(directive).toBeTruthy();
  });
});
