import { MinCharactersDirective } from './min-characters.directive';

describe('MinCharactersDirective', () => {
  it('should create an instance', () => {
    const directive = new MinCharactersDirective();
    expect(directive).toBeTruthy();
  });
});
