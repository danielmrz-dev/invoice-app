import { MinQtyOrPriceDirective } from './min-qty-or-price.directive';

describe('MinQtyOrPriceDirective', () => {
  it('should create an instance', () => {
    const directive = new MinQtyOrPriceDirective();
    expect(directive).toBeTruthy();
  });
});
