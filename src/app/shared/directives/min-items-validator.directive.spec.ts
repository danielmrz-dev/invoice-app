import { MinItemsValidatorDirective } from './min-items-validator.directive';

describe('MinItemsValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new MinItemsValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
